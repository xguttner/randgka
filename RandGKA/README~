This folder has a structure of an Eclipse project using the Android Development Toolkit. To enable the compilation, it is sufficient to import the project into this environment.

An Android installation package RandGKA.apk is included in the ./RandGKA/bin directory.

To obtain the shared key via the app, first the leader has to establish its connection by determining the role (leader), channel and randomness source parameters, and pressing the "Continue" button. Subsequently, the members can connect to it using the same communication channel.

The application was tested on the following list of devices:
Sony XPeria Ray ST18i
HTC Desire A8181
HTC EVO 3D X515m
